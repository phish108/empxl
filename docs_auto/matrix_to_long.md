# Function: `MATRIX_TO_LONG()` `MATRIX.ZU.LANGFORM()` 

# SYNOPSIS 

```
MATRIX_TO_LONG(matrix;[p_headers];[p_withnames])
MATRIX.ZU.LANGFORM(matrix;[namen];[vektor_namen])
```

# DESCRIPTION 

This stupid hack is needed to avoid Field errors. 
