=LAMBDA(
    p_value; [alternative];
    LET(
        alt; WENN(WURDEAUSGELASSEN(alternative); "t"; alternative);
        WENNFEHLER(
            ERSTERWERT(
                EMPXL.PREP.ALT(alt);
                "l"; p_value;
                "g"; 1 - p_value;
                "t"; 2 * ((p_value <=0.5) * p_value + (p_value > 0.5) * (1 - p_value));
            ); 
            #WERT!
        );
    ) 
)
