F.TEST.S=LAMBDA(
    dep_var; indep_var; [headers];
    LET(
        l_uv; ZUSPALTE(EINDEUTIG(indep_var));
        result; WENN(
            ANZAHL2(l_uv) = 2; 
            F.TEST(FILTER(dep_var; indep_var = INDEX(indep_var; 1; 1));FILTER(dep_var; indep_var = INDEX(indep_var; 2; 1)));
            #WERT!
        );
        WENN(ODER(WURDEAUSGELASSEN(headers); NICHT(headers)); result; VSTAPELN("p"; result))
    )
);;