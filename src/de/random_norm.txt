RANDOM.NORM =LAMBDA(
    n_values; 
    [mean];
    [sdev];
    LET(
        mean; WENN(WURDEAUSGELASSEN(mean); 0; mean);
        sdev; WENN(WURDEAUSGELASSEN(sdev); 1; sdev);

        NORM.inv(ZUFALLSMATRIX(n_values);mean;sdev)
    )
);;