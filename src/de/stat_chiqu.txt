STAT.CHIQU= LAMBDA(
    x_i; y_i;
    LET(
        n_i; STAT.TABLE(x_i);
        n_j; STAT.TABLE(y_i);
        n; SUMME(n_j);
        EW; n_i * MTRANS(n_j) / n; 
        n_ij; STAT.TABLE(x_i; y_i);
        SUMME((n_ij - EW) ^ 2 / EW)
    )
);;

