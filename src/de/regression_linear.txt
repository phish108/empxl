REGRESSION=LAMBDA(
    A_VAR; UA_VAR;
    LET(
        KOEFMAT; HSTAPELN(SEQUENZ(ZEILEN(A_VAR);1;1;0); UA_VAR);
        REDDIM; MMULT(MTRANS(KOEFMAT);KOEFMAT);
        MMULT(MMULT(MINV(REDDIM);MTRANS(KOEFMAT));A_VAR)
    )
);;