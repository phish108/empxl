REGRESSION.RESIDUALS=LAMBDA(
    A_VAR; UA_VAR; KOEF;
    LET(
        KOEFMAT; HSTAPELN(SEQUENZ(ZEILEN(A_VAR);1;1;0); UA_VAR);
        ESTIMATES; MMULT(KOEFMAT; KOEF);
        ESTIMATES - A_VAR
    )
);;

RGP.RESIDUALS=REGRESSION.RESIDUALS;;