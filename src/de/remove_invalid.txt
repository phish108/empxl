REMOVE.INVALID=LAMBDA(
    vectors;
    LET(
        invalid; NACHZEILE(
            vectors;
            LAMBDA(
                zeile;
                ODER(ISTFEHLER(zeile))
            )
        );
        FILTER(vectors;NICHT(invalid))
    )
);;