REMOVE.INVALID=LAMBDA(
    vectors;
    LET(
        invalid; WENNFEHLER(
            NACHZEILE(
                J2#;
                LAMBDA(
                    zeile;
                    UND(zeile)
                )
            );
            FALSCH
        );
        FILTER(vectors;invalid);
    )
);;